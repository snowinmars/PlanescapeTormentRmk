label never_characters:
    $ never = _('protagonist_character_name')          # The Nameless One
    $ never = _('protagonist_character_current_class') # Fighter
    $ never = _('protagonist_character_race')          # Human
    $ never = _('protagonist_character_sex')           # Male

    $ never = _('morte_character_name')          # 'Morte'
    $ never = _('morte_character_current_class') # 'Fighter'
    $ never = _('morte_character_race')          # 'Human. Once'
    $ never = _('morte_character_sex')           # 'Male'

    $ never = _('annah_character_name')          # 'Annah'
    $ never = _('annah_character_current_class') # 'Fighter/Thief'
    $ never = _('annah_character_race')          # 'Tieffing'
    $ never = _('annah_character_sex')           # 'Female'

    $ never = _('ignus_character_name')          # 'Ignus'
    $ never = _('ignus_character_current_class') # 'Mage'
    $ never = _('ignus_character_race')          # 'Human'
    $ never = _('ignus_character_sex')           # 'Male'

    $ never = _('grace_character_name')          # 'Fall-From-Grace'
    $ never = _('grace_character_current_class') # 'Cleric'
    $ never = _('grace_character_race')          # 'Succubus'
    $ never = _('grace_character_sex')           # 'Female'

    $ never = _('dakkon_character_name')          # 'Dak’kon'
    $ never = _('dakkon_character_current_class') # 'Fighter/Mage'
    $ never = _('dakkon_character_race')          # 'Githzerai'
    $ never = _('dakkon_character_sex')           # 'Male'

    $ never = _('nordom_character_name')          # 'Nordom'
    $ never = _('nordom_character_current_class') # 'Fighter'
    $ never = _('nordom_character_race')          # 'Modron'
    $ never = _('nordom_character_sex')           # 'Neither'

    $ never = _('vhail_character_name')          # 'Vhail'
    $ never = _('vhail_character_current_class') # 'Fighter'
    $ never = _('vhail_character_race')          # 'Restless Spirit'
    $ never = _('vhail_character_sex')           # 'Male'
